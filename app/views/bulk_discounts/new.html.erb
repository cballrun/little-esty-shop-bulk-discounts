<h3> New Bulk Discount </h3>

<%= form_with model: @bulk_discount, url: merchant_bulk_discounts_path(@merchant), local: true do |f| %>
  <%= f.label :percentage %>
  <%= f.number_field :percentage %>

  <%= f.label :quantity%>
  <%= f.number_field :quantity %>

  <%= f.submit %>
<% end %>